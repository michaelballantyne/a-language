#lang a

(require runtime/runtime compile/parser-tools)

(provide main)

(def g (seq (c "a") (c "b")))

(def main
  (fn (args)
  (def _ (displayln (parse g (string->parse-input "ab" "unknown"))))
  (def _2 (displayln (parse g (string->parse-input "c" "unknown"))))
  "foo"))
